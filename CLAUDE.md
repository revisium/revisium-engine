# CLAUDE.md — Revisium Engine

## Quick Reference

```bash
npm run start:dev       # dev mode
npm run build           # production build
npm run lint:ci         # lint (0 warnings)
npm run tsc             # type check
npm test                # run tests
npm run test:cov        # tests + coverage
```

## Knowledge Base

MCP: `revisium-cloud-io`, org: `revisium-kb`, project: `kb-revisium-engine`

Load context from Revisium cloud when working on this repo.
